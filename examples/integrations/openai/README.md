# OpenAI Integration Example

This folder shows how an application can wire OpenAI on top of DocEX. Install OpenAI dependencies in your application environment and pass an embedding callable into DocEX vector indexing.
