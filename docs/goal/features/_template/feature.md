# Feature: <Feature Name>

Status: `not-started`

## Summary

- `<One or two bullets describing what this feature does.>`

## User Goal

- `<Who needs this and what outcome they expect.>`

## Feature Implementation Goal

- `<Specific implementation goal the agent must confirm with the user before coding.>`

## Required Inputs And Configuration

- `<Input, credential, file, env var, config, feature flag, or dependency.>`

## UI Behavior

- `<Screen, action, state, loading behavior, error behavior, and success behavior.>`
- `<Remove this section if the feature has no UI.>`

## API / Backend Behavior

- `<Route, service, job, worker, adapter, or integration behavior.>`
- `<Remove this section if the feature has no backend.>`

## Data And Persistence

- `<Tables, files, object stores, migrations, cache behavior, audit events.>`
- `<State what must be preserved.>`
- `<State what must not be persisted.>`

## Inputs

- `<Runtime input.>`

## Outputs

- `<Returned values, files, artifacts, statuses, metrics, events, logs.>`

## Failure Behavior

- `<Expected user-facing error.>`
- `<Expected logs or diagnostics.>`
- `<Fallback rules, if any.>`

## Security, Privacy, And Access

- `<Auth, authorization, tenancy, data boundary, secret handling, PII handling.>`

## Success Evidence

- `<Observable proof that the workflow works.>`
- `<Regression proof that old behavior is preserved.>`

## Anchors

- UI: `<path>`
- API/routes: `<path>`
- Services/helpers: `<path>`
- Config: `<path>`
- Tests: `<path>`
- Docs: `<path>`

## Verification

- [ ] `<Command or proof for unit coverage.>`
- [ ] `<Command or proof for integration coverage.>`
- [ ] `<Command or proof for UI/browser coverage.>`
- [ ] `<Command or proof for regression coverage.>`
